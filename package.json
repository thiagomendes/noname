{
  "name": "voice-rooms-app",
  "version": "1.0.0",
  "description": "Aplicação SPA de salas de voz em tempo real",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "generate-certs": "mkdir -p certificates && openssl req -nodes -new -x509 -keyout certificates/key.pem -out certificates/cert.pem -days 365"
  },
  "dependencies": {
    "nedb": "^1.8.0",
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "uuid": "^9.0.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}